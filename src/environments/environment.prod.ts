export const environment = {
  production: true,
  apiUrl: 'https://api.yourproductionurl.com/api', // Replace with your production API URL
  jwtSecretKey: '${JWT_SECRET_KEY}' // Will be replaced during build
};